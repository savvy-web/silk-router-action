name: Local Test
permissions:
  contents: read
  actions: write
on:
  workflow_dispatch: null

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Runtime
        id: runtime
        uses: ./.github/actions/local
